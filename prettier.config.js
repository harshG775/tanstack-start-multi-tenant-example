//  @ts-check

/** @type {import('prettier').Config} */
const config = {
    semi: false,
    singleQuote: false,
    trailingComma: "all",
    tabWidth: 4,
    useTabs: false,
    printWidth: 120,
}

export default config
